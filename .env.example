# 이 파일을 .env 로 복사한 뒤 값을 채우세요 (cp .env.example .env)
# .env 는 .gitignore 처리됨 — 절대 커밋하지 마세요.

# 텔레그램 알림 (없으면 콘솔로 대체됨)
# 1) 텔레그램에서 @BotFather → /newbot → 토큰 발급
TELEGRAM_BOT_TOKEN=
# 2) 봇에게 메시지 한 번 보낸 뒤:  python watch.py --get-chat-id
TELEGRAM_CHAT_ID=

# 감시 주기(분). 기본 30분.
WATCH_INTERVAL_MIN=30
